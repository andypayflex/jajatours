# Admin Authentication
# Generate hash with: node src/db/seed.js
ADMIN_PASSWORD_HASH=$2a$12$examplehashhere

# Session secret (random string, at least 32 chars)
SESSION_SECRET=change-me-to-a-random-string-at-least-32-chars

# Database path (persistent disk on Render)
DATABASE_PATH=./data/jajatours.db

# Upload directory (persistent disk on Render)
UPLOAD_DIR=./data/uploads
