---
import BaseLayout from '@layouts/BaseLayout.astro';
---

<BaseLayout
  title="Contact"
  description="Get in touch with JaJa Tours. Tell us your dates and adventure style and we will build a route concept within 48 hours."
>
  <section class="contact-hero">
    <div class="container">
      <span class="section-label">Start Planning</span>
      <h1>Let's build your route.</h1>
      <p class="contact-lead">
        Tell us your dates, group size, and energy level. We will return a
        route concept within 48 hours.
      </p>
    </div>
  </section>

  <section class="contact-body">
    <div class="container contact-grid">
      <form
        class="contact-form"
        method="POST"
        action="/api/contact"
      >
        <p class="hidden" aria-hidden="true">
          <label>Do not fill this out: <input name="bot-field" /></label>
        </p>

        <div class="form-field">
          <label for="name">Name</label>
          <input type="text" id="name" name="name" required autocomplete="name" />
        </div>

        <div class="form-field">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" required autocomplete="email" />
        </div>

        <div class="form-field">
          <label for="interest">What excites you?</label>
          <select id="interest" name="interest">
            <option value="">Pick a style...</option>
            <option value="overlanding">Overlanding</option>
            <option value="diving">Diving &amp; Coastal</option>
            <option value="rafting">River &amp; Canyon</option>
            <option value="camping">Wild Camping</option>
            <option value="custom">Something Custom</option>
          </select>
        </div>

        <div class="form-field">
          <label for="message">Tell us more</label>
          <textarea id="message" name="message" rows="5" placeholder="Dates, group size, must-see places, fitness level — anything helps."></textarea>
        </div>

        <button type="submit" class="button-primary">Send Inquiry</button>
      </form>

      <div class="contact-aside">
        <div class="aside-card">
          <h3>Prefer a quick chat?</h3>
          <p>
            Reach us on WhatsApp for a faster back-and-forth. We are usually
            online during South African business hours.
          </p>
          <a
            href="https://wa.me/27123456789"
            class="button-secondary"
            target="_blank"
            rel="noopener noreferrer"
          >
            WhatsApp Us
          </a>
        </div>

        <div class="aside-card">
          <h3>Email directly</h3>
          <p>
            <a href="mailto:info@jajatours.co.za">info@jajatours.co.za</a>
          </p>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .hidden {
    display: none;
  }

  .contact-hero {
    padding: 0.7rem 0 2.5rem;
  }

  .contact-hero h1 {
    margin: 0.95rem 0 1.05rem;
    max-width: 16ch;
  }

  .contact-lead {
    max-width: 52ch;
    font-size: 1.1rem;
  }

  .contact-body {
    padding: 0 0 3rem;
  }

  .contact-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.6rem;
  }

  .contact-form {
    display: grid;
    gap: 1rem;
  }

  .form-field {
    display: grid;
    gap: 0.35rem;
  }

  .form-field label {
    font-weight: 600;
    font-size: 0.92rem;
    color: var(--color-ink);
  }

  .form-field input,
  .form-field select,
  .form-field textarea {
    width: 100%;
    min-height: 48px;
    padding: 0.65rem 0.85rem;
    border-radius: var(--radius-sm);
    border: 1px solid var(--color-card-border);
    background: rgba(255, 255, 255, 0.84);
    font: inherit;
    font-size: 1rem;
    color: var(--color-ink);
    transition: border-color 180ms ease, box-shadow 180ms ease;
  }

  .form-field input:focus,
  .form-field select:focus,
  .form-field textarea:focus {
    outline: none;
    border-color: var(--color-reef);
    box-shadow: 0 0 0 3px rgba(47, 143, 157, 0.15);
  }

  .form-field textarea {
    resize: vertical;
    min-height: 120px;
  }

  .contact-aside {
    display: grid;
    gap: 0.8rem;
    align-content: start;
  }

  .aside-card {
    padding: 1.1rem;
    border-radius: var(--radius-md);
    border: 1px solid var(--color-card-border);
    background: rgba(255, 255, 255, 0.74);
    box-shadow: 0 10px 24px rgba(18, 36, 51, 0.08);
  }

  .aside-card h3 {
    margin-bottom: 0.45rem;
    font-size: 1.2rem;
  }

  .aside-card p {
    margin: 0 0 0.8rem;
  }

  .aside-card p:last-child {
    margin-bottom: 0;
  }

  .aside-card a[href^="mailto"] {
    color: var(--color-deep);
    font-weight: 600;
  }

  @media (min-width: 980px) {
    .contact-grid {
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 0.8fr);
      align-items: start;
    }
  }
</style>
